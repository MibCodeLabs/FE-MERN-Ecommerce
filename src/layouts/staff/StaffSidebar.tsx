import { Box } from "@mui/material";

export default function StaffSidebar() {
    return (
        <Box
            component="aside"
            sx={{
                width: 240,
            }}
        />
    );
}
